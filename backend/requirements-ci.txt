# CI-only dependencies — never bundled in Lambda.
# Runtime deps are in requirements.txt.

# Linting & formatting
ruff==0.11.13

# Security scanning
bandit[toml]==1.9.4
pip-audit==2.10.0

# Test coverage
coverage[toml]==7.14.1
